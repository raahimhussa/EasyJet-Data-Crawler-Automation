# EasyJet Data Crawler

## Setup
1. Install Node.js (v16 or higher).
2. Run `npm install playwright axios`.
3. Place the `jetjobs.txt` file in the project root.
4. Run the crawler: `node crawler.js`.

## Output
- JSON responses are saved in the `output/` directory.
- Errors are logged in `output/errors.log`.

## Scheduling
- Schedule daily runs using `cron` (e.g., `0 1 * * * node /path/to/crawler.js`).

## Notes
- Adjust `concurrency` in `crawlAllURLs` to balance speed and rate limiting.
- If blocked, consider adding a proxy in `initBrowser`.